PORT=3043
GENERATE_SOURCEMAP=false
# Local backend. On Render, set REACT_APP_API_URL to your backend URL (build-time).
# Must match this PC's current Wi‑Fi IP (ipconfig). Restart `npm start` after changing.
REACT_APP_API_URL=http://192.168.1.54:3000

# Hot reload on Windows / OneDrive (file watchers often miss changes)
FAST_REFRESH=true
CHOKIDAR_USEPOLLING=true
WATCHPACK_POLLING=true