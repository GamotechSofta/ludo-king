MONGO_URL=
PORT=3000
# Comma-separated allowed frontend origins (CORS + WebSocket)
CLIENT_URL=http://localhost:3043,https://ludo-king-frontend.onrender.com
SESSION_SECRET=ludo-dev-session-secret-change-me
OAUTH_ENABLED=false
GOOGLE_KEY=
GOOGLE_SECRET=
GITHUB_KEY=
GITHUB_SECRET=
# Optional: redis://localhost:6379 or rediss://... (Upstash/Render Redis)
REDIS_URL=

# --- Aakda platform (WebView launch) ---
# Extra CORS parents (iframe / platform web)
CORS_ALLOWED_ORIGINS=https://www.aakda.in,https://aakda.in,http://localhost:5173
# If set, require header X-Platform-Key on /api/platform/balance|debit|credit
PLATFORM_SHARED_SECRET=
